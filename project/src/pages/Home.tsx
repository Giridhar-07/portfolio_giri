import React from 'react';
import Hero from '../components/Hero';

/**
 * Home Page
 * Displays the landing hero section and can be extended with highlights.
 */
const Home: React.FC = () => {
  return <Hero />;
};

export default Home;