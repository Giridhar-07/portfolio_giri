import React from 'react';
import Contact from '../components/Contact';

/**
 * Contact Page
 * Wraps the Contact section for route-based navigation.
 */
const ContactPage: React.FC = () => {
  return <Contact />;
};

export default ContactPage;