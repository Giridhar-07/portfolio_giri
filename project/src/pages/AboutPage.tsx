import React from 'react';
import About from '../components/About';

/**
 * About Page
 * Wraps the About section for route-based navigation.
 */
const AboutPage: React.FC = () => {
  return <About />;
};

export default AboutPage;