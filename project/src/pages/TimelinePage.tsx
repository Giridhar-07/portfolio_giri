import React from 'react';
import Timeline from '../components/Timeline';

/**
 * Timeline Page
 * Wraps the Timeline section for route-based navigation.
 */
const TimelinePage: React.FC = () => {
  return <Timeline />;
};

export default TimelinePage;