import React from 'react';
import Certificates from '../components/Certificates';

/**
 * Certificates Page
 * Wraps the Certificates section for route-based navigation.
 */
const CertificatesPage: React.FC = () => {
  return <Certificates />;
};

export default CertificatesPage;