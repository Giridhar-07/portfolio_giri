import React from 'react';
import Skills from '../components/Skills';

/**
 * Skills Page
 * Wraps the Skills section for route-based navigation.
 */
const SkillsPage: React.FC = () => {
  return <Skills />;
};

export default SkillsPage;